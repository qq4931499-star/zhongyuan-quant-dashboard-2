# 中圆量化收益分析仪表板：备份、恢复与迁移方案

**盘点日期：** 2026-08-16（SGT）  
**适用对象：** 当前 `zhongyuan-quant-dashboard` 网站及其公开协作交易数据。  
**重要前提：** 是否受本次数据分离影响，应以每个个人或团队账户收到的 **应用内通知与邮件** 为准；本文不据项目所在地或创建时间推断账户类型。[1]

> **直接结论：** 如果账户受影响，应在 **2026-08-23 07:59 SGT 前**完成最新的 **Task Data Backup**。这才是能够恢复本网站代码、数据库、静态文件、配置和网站依赖的主备份。代码下载或截图都不能替代该备份。[2] [3]

## 1. 当前网站功能与数据资产盘点

本网站是公开协作型量化收益仪表板，任何访客均可查看、编辑交易数据和导出图片。当前功能和资产可按下表划分。

| 范畴 | 当前内容 | 主要载体 | 备份优先级 | 恢复或迁移关注点 |
|---|---|---|---|---|
| 交易明细 | 新增、编辑、删除、分钟级买卖时间、未平仓记录、收益率计算 | `trades` 数据表 | 极高 | 这是业务源数据；要保留全部字段和记录 ID 对应关系。 |
| 仪表板设置 | 标题、副标题、展示统计区间 | `dashboardSettings` 数据表 | 高 | 单行配置，但决定页面与导出图片的品牌文案和区间。 |
| 身份模板数据 | 平台用户身份及角色字段 | `users` 数据表 | 中 | 当前网站无需登录即可编辑，但全栈模板仍保留此表。 |
| 批量导入 | CSV/XLSX 模板、解析、校验、重复交易保护 | 前后端代码与交易数据 | 高 | 恢复后需测试模板下载、空卖出字段和重复交易跳过。 |
| 导出能力 | 营销图、策略汇总海报、今日策略战报；日期范围和超长图片保护 | 前端代码、样式、图片资源 | 高 | 恢复后要检查 PNG 下载、日期范围和长明细底部完整性。 |
| 股票识别 | 代码、名称、拼音缩写候选与精确补全 | 前端/服务端代码；公开 HTTP 查询 | 中 | 查询结果不落库；恢复或迁移时需检查外网访问与候选接口可用性。 |
| 品牌资源 | 公司 Logo、白色 Logo、策略海报背景图 | `/manus-storage/` 资源引用 | 高 | Task Data Backup 应保留上传资源；另存原始文件可作为补充。 |
| 域名与部署 | 当前默认站点 `quantdash-gkdfnrt3.manus.space` | 项目部署与域名配置 | 高 | 默认 `manus.space` 地址不可作为外部迁移 DNS 入口。 |

当前数据库只读盘点结果为：`trades` **38** 条交易，买入时间范围为 **2026-07-31 13:23 至 2026-08-14 13:48**，其中 **3** 条卖出信息未完整；`dashboardSettings` 为 **1** 条，`users` 为 **1** 条。该数字是盘点时点快照，后续公开协作编辑会改变它。

## 2. 主备份方案：Task Data Backup

对于受影响账户，建议把 **Task Data Backup** 作为唯一的可恢复主备份。官方说明指出，网站任务备份包含网站代码、静态资源、数据库内容、网站配置、密钥/环境变量及已配置的第三方集成；仅下载源码不包含数据库、上传文件和平台托管能力。[2]

| 操作阶段 | 建议动作 | 完成标准 |
|---|---|---|
| 立即执行 | 打开 [Data Backup Tool](https://manus.im/backup)，选择 **Export task data → Export more → Custom export → Website tasks → All time**。如需覆盖同一账户的所有任务，选择 **All tasks → All time**。 | 导出任务已完成，备份包已实际出现在目标位置。 |
| 备份窗口内 | 因该站允许多人公开编辑，建议在每次集中导入/更新交易后重新导出；至少保留一份初始快照和一份截止前最新快照。 | 每个完整备份集保留在独立、可访问的位置。 |
| 截止前 | 在业务数据最后一次更新后，生成最终 Task Data Backup。 | 最终包时间晚于最后一次业务修改。 |
| 归档 | 优先保存至 Google Drive 或 OneDrive，并在本地或第二个受控位置保留完整副本。不要改名、拆分或混用不同导出批次的分包。 | 能看到完整包；包名和分包关系保持原状。 |

所有 Task Data Backup 都是**时间点快照**，不会随着后续交易编辑自动同步。因此，备份生成后新增或修改的交易不会进入旧包；公开协作站点尤其应在最后一次数据变更后再做一次新导出。[2] [3]

如果应用内通知显示账户为 **Type C**，还必须先生成 **Account Data Backup**，再生成 Task Data Backup；恢复时也必须先恢复账户，再恢复任务。Type A/B 通常只需要 Task Data Backup。[1] [2]

## 3. 补充备份：可读业务数据与运行清单

Task Data Backup 用于可恢复性；同时建议建立一份团队可读的业务连续性资料。它不能替代官方备份，但能加快核验和未来迁移。

| 补充材料 | 建议格式 | 建议频率 | 用途 |
|---|---|---|---|
| 全量交易账本 | CSV 与 XLSX，各含代码、名称、买卖价、买卖时间 | 每次重要更新后；截止前最终一次 | 未来异平台导入、人工核对、争议追溯。 |
| 设置快照 | JSON/Markdown：标题、副标题、统计区间、默认导出设置 | 配置变更后 | 恢复后快速比对页面文案。 |
| 数据核验清单 | CSV/Markdown：交易条数、最早/最晚买入时间、未平仓数量 | 与每次主备份同时生成 | 恢复后做快速完整性校验。 |
| 品牌原始文件 | 原始 Logo 与海报背景图 | 资源替换时 | 防止只保留已压缩或远程引用版本。 |
| 代码副本 | 项目 ZIP 或 Git 仓库快照 | 每个稳定版本后 | 便于审计和长期外部迁移准备。 |

当前应用已支持交易的 CSV/XLSX **导入**与模板下载，但尚未提供面向管理员的“全量原始交易账本导出”按钮。若团队要把异平台迁移作为常规预案，建议下一步新增受控的 CSV/XLSX 全量备份下载功能，并将数据校验清单一并生成。

## 4. 恢复方案与恢复后验收

若账户受影响，恢复入口在 **2026-08-25 08:00 SGT** 开放。恢复只能完成一次，因此上传前应确认所选包完整且包含最新快照；可一次上传多个完整包，由平台去重整合。[4]

| 顺序 | 恢复动作 | 本项目验收标准 |
|---|---|---|
| 1 | Type C 先恢复账户；Type A/B 直接登录与恢复任务数据。 | 使用与备份匹配的账户/团队空间。 |
| 2 | 在 Data Backup Tool 上传最新且完整的 Task Data Backup。 | 网站任务、代码、资源和数据库恢复完成。 |
| 3 | 等待已部署网站自动重新可用。 | 访问 `quantdash-gkdfnrt3.manus.space`，首页正常打开。 |
| 4 | 检查数据库 | 与最终备份清单对比交易条数、最早/最晚买入时间、未平仓数量、页面标题。 |
| 5 | 检查关键业务流程 | 新增/编辑交易、卖出时间确认、收益率、股票候选、CSV/XLSX 导入均可正常运行。 |
| 6 | 检查导出 | 当日、本周、本月、全部、自定义范围分别能生成营销图与策略海报；长明细底部完整。 |
| 7 | 检查资源与域名 | 三张品牌资源加载成功；默认域名恢复可访问。若日后绑定自定义域名，再检查域名绑定。 |

官方说明指出，已部署网站在恢复 Task Data Backup 后会自动重新部署；第三方连接器需要手工重新启用。[4] 当前股票候选功能使用直接 HTTPS 查询，不是已配置的连接器，但仍应在恢复后验证外部查询可用。

## 5. 迁移方案：区分“恢复”与“长期外迁”

对于可能受影响的网站，**优先使用 Task Data Backup 恢复到 Manus**。官方没有提供用于约两天数据删除期的完整异平台迁移方案；仅有源码副本不足以带走数据库、文件和平台托管能力，也不建议仅为了短期中断而转移部署或域名所有权。[5]

如果团队有长期迁出 Manus 的战略需求，可在正常服务期单独做一项计划迁移，而不是把它当作短期恢复替代。建议遵循以下顺序：

1. 先完成官方 Task Data Backup，确保具备原平台恢复能力。
2. 新增并下载全量交易 CSV/XLSX、设置 JSON、品牌原始文件和代码快照。
3. 在目标环境创建 MySQL 数据库，按现有 `users`、`dashboardSettings`、`trades` 表结构建表，再导入经过校验的业务数据。
4. 将静态资源复制到目标对象存储，配置新的环境变量、数据库连接和股票候选查询的出网策略。
5. 在临时域名完成交易编辑、批量导入、三类图片导出、长图片边界、日期范围和股票候选测试。
6. 若使用自有域名，只在目标站验收通过后调整 DNS；若使用当前默认 `manus.space` 地址，则不能作为外部迁移 DNS 入口。[5]

## 6. 现在最值得执行的操作

请先查看该账户和团队空间的应用内通知/邮件，确认是否受影响以及账户类型。若受影响，今日先完成一份 **All tasks / All time** 或 **Website tasks / All time** 的 Task Data Backup；在之后每次交易集中更新后做增量时间点备份，并在 2026-08-23 07:59 SGT 前完成最终快照。对于团队空间，只有团队所有者可以创建和恢复团队备份。[2] [4]

从业务连续性角度，建议下一项产品优化是增加“**全量交易账本导出 + 数据核验清单**”。这将提供可读的离线副本，并为长期外迁预案补足当前只有导入、没有原始全量业务导出的缺口。

## References

[1] [What’s Happening and Am I Affected?](https://help.manus.im/en/articles/16147831-service-change-overview-what-s-happening-and-am-i-affected)  
[2] [How to Back Up Your Data](https://help.manus.im/en/articles/16147892-service-change-overview-how-to-back-up-your-data)  
[3] [A Note to Our Users](https://manus.im/blog/a-note-to-our-users)  
[4] [How to Restore Your Data](https://help.manus.im/en/articles/16147895-service-change-overview-how-to-restore-your-data)  
[5] [Websites During the August 2026 Data Separation](https://help.manus.im/en/collections/19704025-data-back-up-and-restoration)
