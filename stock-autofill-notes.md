# 股票代码与名称自动补全设计记录

## 已确认范围

自动补全仅在股票代码与股票名称之间互相补齐；不会修改买入价、卖出价、买入时间、卖出时间或其他交易字段。

## 检索验证

已验证腾讯财经公开智能检索端点可按六位股票代码、中文名称片段和拼音缩写返回候选项。例如，`600376` 与“首开”均返回 `600376 / 首开股份`；`000001` 会同时返回指数、股票和基金，因此实现中必须仅保留 `GP-A`（A 股股票）候选项，并优先选择与输入六位代码完全一致的记录。

## 匹配规则

对六位代码输入，仅在存在唯一的 A 股精确代码匹配时自动写入股票名称。对中文名称输入，仅在存在唯一的 A 股精确名称匹配时自动写入股票代码。部分名称、拼音缩写或多个候选项不自动覆盖现有值，保留用户手工编辑，避免误匹配。
